<h1><%= @posts.count %> found</h1>

<ul>
  <% @posts.results.each do |post| %>
    <li>
      <h3>
        <%= link_to post.try(:highlight).try(:title) ? post.highlight.title[0].html_safe : post.title,
          controller: "posts",
          action: "show",
          id: post._id%>
      </h3>
      <% if post.try(:highlight).try(:text) %>
        <% post.highlight.text.each do |snippet| %>
          <p><%= snippet.html_safe %>...</p>
        <% end %>
      <% end %>
    </li>
  <% end %>
</ul>